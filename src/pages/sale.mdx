import ArticleCourseCard from '../components/blog/article-course-card'
export const meta = {
  title: `A Sale Page`,
}

import TestProseLayout from 'layouts/test-prose'
export default ({children}) => (
  <TestProseLayout meta={meta}>{children}</TestProseLayout>
)

# Hello and welcome

We've got some text here.

<ArticleCourseCard course={'react-state-management-in-2021-6732'} />
