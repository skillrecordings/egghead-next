export const meta = {
  title: `Humans of egghead`,
}

import DefaultLayout from 'layouts'
export default ({children}) => (
  <DefaultLayout meta={meta}>{children}</DefaultLayout>
)

These are our stories.
