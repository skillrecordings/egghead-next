# Egghead Next Vision

This is the retire-child of the **migrate-egghead root vision**. `egghead-next` is a compatibility and load-shedding surface while Rails exits. It is not the destination app.

## Do Now

- Keep necessary compatibility and load-shedding paths safe through cutover.
- Keep narrow commercial and marketing paths working while CourseBuilder commerce takes ownership.
- Keep flags defaulting to Rails/current behavior until the approved flip.

## Do Not Do

- Add new product surface.
- Chase standalone-app parity or recreate legacy dynamic chrome.
- Build a headless-Rails-billing afterlife.

This repo retires at cutover, targeted in weeks, not quarters. The root vision and `AGENTS.md` win if they conflict with this file; Joel approves amendments.
